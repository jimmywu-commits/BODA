# MSBN 版位修正紀錄（2026-08-13 ／ 08-14）

依據新版 `MSBN_css.txt`（Copy All Layers CSS）所做的兩輪修正。
本檔說明「改了什麼、為什麼這樣改、怎麼驗證」，之後要再調整時可以直接接手。

- 第一輪：`msbn-css-v12.txt`（2026-08-13 17:18 匯出）→ 見第 1～7 節
- 第二輪：`msbn-css-v13.txt`（2026-08-14 11:42 匯出）→ 見第 8 節

---

## 0. 先回答：新版 CSS 有沒有幫助？

**有，而且是關鍵性的。** 舊版匯出少了三樣東西，新版都補上了：

| 新版多出來的資訊 | 影響 |
|---|---|
| `border-radius` | 舊版沒有這一項，所以產生 block.json 時圓角整批遺失，A-2-4、B-1-x、B-2-x、B-3-3/4、C、D 系列的圓邊全都變直角。這次全部補回來了。 |
| GROUP 樹狀結構（左／中／右、共用） | 像 C-1-4 這種一格三欄的版位，終於知道哪一段文字屬於哪一欄，不用再猜。 |
| 圖層原文 `/* content: "..." */` | 可以用原本的示意文字去對應欄位，也讓文字定位可以精準計算。 |

另外還發現一件更重要的事：

> **新版 CSS 的文字 `top` 是「筆畫上緣」，不是文字框上緣。**

用 200 個文字圖層跟參考圖對照驗證過：差值中位數 +1px、標準差 0.78px，非常穩定
（驗證程式：`tools/check_css_ink_hypothesis.py`）。

這代表文字位置可以**算出來**，不用再靠肉眼微調：

```
瀏覽器該用的 top = 筆畫上緣 − （該字串最高筆畫離內容區上緣幾 em）× 字級
```

後面那一項可以直接從字型檔算（`hhea.ascent − 該字 yMax ÷ unitsPerEm`），是精確值不是估值。

**效果（對照 PS 參考圖實測，程式：`tools/verify_ink_accuracy.py`）：**

| | 圖層數 | 絕對誤差平均 | 誤差 ≤1px |
|---|---|---|---|
| 改用精確定位（MSBN） | 176 | **0.89 px** | **91%** |
| 仍用舊估算（副區，沒有新 CSS） | 217 | 3.12 px | 41% |

舊估算的誤差會隨字級放大（實測 40px 差 0～2.4px、45px 差 2.5px、55px 差 7.1px），
這正是「C-1-1～C-1-3 促標字往上跑」的原因——它是全站字級最大的促標（55px）。

⚠️ **要注意的副作用**：所有 MSBN 版位的文字都往正確位置移動了，
包含之前目視確認過的 A-1-1（促標 +2.3px、品名 −2.9px、警語 +6.3px）。
移動後 A-1-1 的促標與品名跟參考圖的誤差是 **0.0px**，但畫面確實跟之前不一樣，請再看一眼。

---

## 1. 圓角補回

由 `tools/apply_css_radius.py` 從新版 CSS 自動搬回，只動圓角、不動座標，
所以先前微調過的位置不會被推翻。

| 版位 | 補回的圓角 |
|---|---|
| A-2-4、A-3-2 | 背景 `20px 20px 0 0`（跟卡片統一的 15px 不同，標 `psRadius` 保護） |
| B-1-1～B-1-3 | 曝品範圍 `15px 0 0 15px`；B-1-4 曝品範圍 `15px` |
| B-1-3、B-1-4 | 促標底 `45px` |
| B-2-1～B-2-3 | 促標底 `15px 15px 0 0` |
| B-3-3 | LOGO 底圖 `15px 15px 0 0` |
| B-3-4 | 曝品範圍 `15px 15px 0 0` |
| C-1-1、C-1-2 | LOGO 圓底 `60px`；C-1-5 `44px` |
| C-1-5 | 曝品範圍 `15px 180px 180px 15px`（左邊滿版、右邊整個半圓） |
| D 系列 | 背景 `15px`、項目欄 `15px 0 0 0` / `0 0 0 15px`、D-3-1 首尾列 |
| 全系列 | CTA 底 `35px` |

渲染引擎同時修掉一個相關的 bug：以前卡片背景一律被畫成「四個角都圓」，
現在改成**保留 block.json 的圓角形狀、只用設定檔控制圓幾 px**
（`scaleRadiusPattern()`），所以「只圓左邊兩角」的曝品範圍不會再凸出卡片。

---

## 2. LOGO 圖框

- **LOGO 上面的「LOGO」佔位字不再顯示**（58 個圖層標 `hidden`，資料保留沒有刪）。
- **LOGO 圖框加上裁切**（62 個圖層標 `clipImage`）：滾輪放大時圖片會被裁在框內，
  而且連框的圓角一起吃，不會溢出去蓋到卡片其他內容。
- 其他圖片框（商品圖等）維持原本「可以超出框」的行為，沒有改。

---

## 3. 維修畫布可以拖圖了

維修頁本來就跟匯入工單頁共用同一套畫布互動，拖不進去的真正原因是：

> LOGO 佔位字那一層整片蓋在 LOGO 圖框上面，圖片拖過去時落在文字圖層上，
> 圖框根本收不到 drop 事件。

修法有兩層，兩層都做了：

1. 佔位字本身已經不顯示（上一節）。
2. 渲染引擎現在讓**所有純裝飾圖層不擋滑鼠**（`pointer-events:none`）：
   沒有綁欄位的文字、以及全部的色塊／圓形。這類圖層本來就沒有任何互動，
   擋住滑鼠只會讓底下的圖片框收不到事件。

---

## 4. C 系列優惠券重做

舊的 block.json 只留下白底跟色塊兩個方塊，券的造型全都沒有。
現在由 `tools/build_c_series.py` 依 CSS 座標 + 參考圖實測參數重新產生：

- **券底形狀**：白色券底 R6 + 陰影，左側色塊 `6px 0 0 6px`
- **上下半圓缺口**：C-1-1～1-3 半徑 7、C-1-4/1-5 半徑 5，正好卡在券的上下緣。
  顏色綁 `bgColor`，換卡片底色時缺口會跟著換，不會露餡。
- **虛線**：用 `repeating-linear-gradient` 畫（段長 3px、間距 9.5px，實測值），
  縮放時不會有鋸齒。
- **蝦皮 LOGO 保留**：蝦皮購物／商城／直營 3C 三顆，從參考圖 1:1 裁切去背
  （`tools/crop_shopee_logos.py`，白色圖案疊在單色色塊上，用線性去背，沒有紅邊）。
  以 data URL 內嵌在 block.json，所以就算 index.html 是直接用檔案總管點開、
  或資料夾被搬動，下載成品 PNG 時 LOGO 也不會消失。
  這個位置同時是可拖放的圖片欄位，想換成別的品牌 LOGO 直接拖進去就好。
- **LOGO 圓框**（C-1-1/1-2/1-5）：可拖圖、放大裁切在圓框內、上面的「LOGO」字拿掉。
- **「＋」**：改用兩個白色方塊拼出來，不依賴字型。
- **C-1-4 分左／中／右**：三欄的促標、券文案、CTA 各自照自己那一欄的座標定位
  （以前三行促標都是整條 1160 寬置中，疊在一起）。右欄「信用卡圖」改成可拖放的圖片框。
- **C-1-5 左側曝品範圍**：`15px 180px 180px 15px`，左邊切齊卡片、右邊是完整半圓（1:1），
  而且是可拖放的商品圖框，圖片放大會裁在這個形狀裡面。

> 註：C-1-5 中間那張券，PSD 圖層名是「LOGO_蝦皮商城」，但參考圖上實際印的是
> **蝦皮購物**。這裡以參考圖為準。

---

## 5. D-1-3 / D-2-3 排版對齊

D-1-3 的第 1～4 列內文被寫成「整條 1160 寬、靠左 20」的滿版文字，
只有第 5 列是正確的分欄座標，所以看起來跟 D-1-2 不一樣。
D-2-3 對 D-2-2 也是同樣的問題。

修法：把每一組（content1~5 / 6~10 / 11~15）的第 1～4 列，
照該組第 5 列的 `left` / `width` / `_refCenter` 對齊（`tools/patch_blocks_v13.py`）。
列高（D-1-2 是 5×80、D-1-3 是 5×77）跟底紋位置是設計上本來就不同的，沒有動。

---

## 6. 新增的工具（都可以重複執行）

| 檔案 | 用途 |
|---|---|
| `tools/parse_msbn_css.py` | 把 PS 匯出的 CSS 轉成結構化 JSON（`tools/msbn-css.json`） |
| `tools/diff_css_vs_blocks.py` | 比對新 CSS 與現有 block.json 的座標差異 |
| `tools/apply_css_radius.py` | 把 CSS 的圓角搬回 block.json |
| `tools/apply_css_ink_top.py` | 用 CSS 筆畫上緣 + 字型檔算出精確的文字位置（`topExact`） |
| `tools/check_css_ink_hypothesis.py` | 驗證「CSS 的 top 就是筆畫上緣」這個前提 |
| `tools/verify_ink_accuracy.py` | 拿參考圖當標準答案，量測文字位置準不準 |
| `tools/build_c_series.py` | 重新產生 C 系列 block.json |
| `tools/crop_shopee_logos.py` | 從參考圖裁切去背蝦皮 LOGO |
| `tools/raster_preview.py` | 把引擎輸出畫成圖片，跟參考圖上下並排比對（`tools/_preview/`） |
| `tools/dump_layout.js` | 印出引擎實際算出來的座標／圓角／裁切設定 |

驗證流程建議：

```bash
python3 tools/parse_msbn_css.py msbn-css-v12.txt tools/msbn-css.json
python3 tools/verify_ink_accuracy.py          # 文字位置準確度
python3 tools/raster_preview.py --all-c       # 看圖比對
node tools/build-qa-preview.js                # 產生 排版驗證.html，用瀏覽器疊圖檢查
```

---

## 7. 還沒處理 / 建議下一步

- **副區（subarea）版位還是用舊估算定位**，誤差平均 3.1px，最大約 10px（警語那一類最明顯）。
  副區沒有對應的新版 CSS，如果你能匯出一份副區的 v1.2 CSS，就能用同一套流程一次修好。
- `tools/verify-text-layout.js` 的「圓標字是否落在圓標圈內」永遠報 0 個，
  這是它把文字框的上下 padding 當成溢出，屬於既有的誤判，跟這次的修改無關。
- 專案根目錄還留著 `editor-plugin.js`、`image-layout.js`、`render-config.js` 的舊副本
  （實際載入的是 `JS/` 底下那份），建議找時間清掉，避免改錯檔案。

---

# 8. 第二輪修正（2026-08-14，依 `msbn-css-v13.txt`）

## 8.1 曝品範圍全部補成可以拖圖片的框（34 個）

設計稿標「曝品範圍」的地方，就是這一格要放商品圖／情境圖的位置。
但盤點後發現有 **34 個**這種區域在 block.json 裡只是一塊 rect 色塊——
畫面上看起來一樣，圖片卻拖不進去、匯入工單也吃不到圖。

| 版位 | 補了幾個 |
|---|---|
| B-4-1、B-4-2 | 各 4 個（你問的就是這兩個，設計稿確實有） |
| B-2-1、B-2-2、B-2-3 | 各 2 個 |
| B-1-1、B-1-2、B-1-3、B-1-4 | 各 1 個（原本是整片底色 rect，就地改成圖片框） |
| D-1-1-1、D-1-1-2 | 各 2 個 |
| D-2-1-1、D-2-1-2 | 各 3 個 |
| A-2-1、A-3-1 | 各補齊到 2 / 3 個 |

做法上有兩種：

- **原本就有一塊尺寸一樣的底色 rect**（B-1-x）→ 就地改成 image 圖層，
  原本的顏色欄位改掛在新的 `bgField` 上，所以底色還是可以在匯入頁改，
  而且放了圖之後底色會留著（圖是疊在色塊上，不是取代它）。
- **設計稿上那塊本來是透明的**（B-2-x、B-4-x、D-x）→ 插一層新的圖片框，
  不給底色，空的時候只顯示一張淡淡的虛線佔位卡提示「這裡可以拖圖片」，
  放了圖就消失，不會平白多出一塊色塊。

欄位編號統一成專案慣例「**由右到左**」：最右邊是 `productImg`，往左 `productImg2`、`3`、`4`。
這樣匯入工單時，同一直行的商品圖／促標／CTA 才會對到同一欄。
A-2-1、A-3-1 原本的 `productImg` 指到最左邊那格，已一併重排（`productImg` → `productImg2` / `productImg3`）。
B-3-4 原本叫 `sceneImg` 的欄位也改名成 `productImg`——舊名字工單根本對不到（工單的「商品」「商品圖」只會對到 `productImg`），改完才吃得到圖。

## 8.2 放大裁切的範圍

只有你指定的那幾格會「放大也不超出色塊」：**B-1-1～B-1-4 的曝品範圍**、**B-3-4 的情境圖**。
這些版位的曝品範圍在設計稿上是一塊看得到的色塊，圖片溢出去會直接蓋到旁邊內容。

其他版位的曝品範圍只是設計稿上的參考框、畫面上沒有邊界，硬裁反而會讓人搞不清楚
圖為什麼被切掉，所以維持「可以超出」。
要改的話：`tools/add_exposure_frames.py` 最上面的 `CLIP_BLOCKS` 加版位 id，重跑一次就好。

同時把設計稿的示意字（「情境圖／商品圖」「商品圖／人物圖／情境圖」）標成 hidden——
那是給設計看的標註，不是要輸出的內容。

## 8.3 B-1-3 / B-1-4 整組重建

這兩格差最多，原因不只是座標偏掉：

1. **文案跑到整條置中**：設計稿上「文案最多10個字以內」是靠右半邊、寬 438、置中；
   舊的 block.json 卻寫成 left 20、寬 1160，整句話往左偏了 **290px**。
2. **白色圓角膠囊被當成促標底色**：那塊 300×90、圓角 45 的白膠囊，
   設計稿上是放 LOGO 的框（裡面就是「LOGO」示意字，參考圖上印的也是 LOGO），
   舊做法卻做成「促標底色」色塊，既不能放 LOGO，改促標顏色時還會把它染色。
   現在改成可拖放的 LOGO 圖框（放大裁切在膠囊內、上面的字不呈現）。
3. **B-1-4 幾乎整格沒做**：只有背景跟代言人小字，LOGO／文案／CTA 全都缺。

已用 `tools/build_b1_34.py` 依新 CSS 重建。

> ⚠️ `msbn-img/MSBN-B-1-4.jpg` 這張參考圖比新版 CSS 舊，上面沒有 LOGO／文案／CTA。
> 這次是以 CSS 為準（CSS 是 8/14 匯出的、圖是前一版）。如果 B-1-4 本來就該是空白那版，
> 跟我說一聲，把那三個元素拿掉就好。

## 8.4 B-1-1 / B-1-2 的品名行距

實際量過了：**品名的行距就是 48px，沒有超過。**

- PS 參考圖上兩行品名的筆畫上緣相差 **48px**
- 渲染引擎畫出來也是 **48px**（block.json 存 1.116 × 43px 字級 = 47.99px）

另外寫了 `tools/check_line_height.py` 把全部 MSBN 文字圖層的行距跟 CSS 對了一遍，
只找到一個真的對不上的：**B-1-1 的警語**，block.json 是 30px、CSS 是 60px，已改正。

如果你看到的是「在畫布上按 Enter 打成兩行、放開之後又變回一行」，那是另一個 bug，
這次一起修了：畫布上的可編輯文字原本用 `textContent` 讀回內容，
而瀏覽器在 contenteditable 裡是用 `<div>`／`<br>` 標籤斷行的，`textContent` 會把換行整個吃掉。
改成用 `innerText` 讀，兩行的品名／促標才存得回去（`readEditableText()`）。

## 8.5 這一輪新增的工具

| 檔案 | 用途 |
|---|---|
| `tools/audit_exposure_areas.py` | 盤點曝品範圍有沒有做成圖片框（改完應該顯示「要補 0 個」） |
| `tools/add_exposure_frames.py` | 自動補上／就地轉換曝品範圍圖片框 |
| `tools/build_b1_34.py` | 依 CSS 重建 B-1-3 / B-1-4 |
| `tools/check_line_height.py` | 比對行距，`--write` 可直接改成跟 CSS 一致 |

`tools/raster_preview.py` 也升級了：可以指定欄位內容，
所以能「放一張測試圖進去」看圖片實際會落在哪、有沒有被裁到。

## 8.6 要注意的行為變化

- **代言人小字沒填時會消失**：B-1-1～B-1-4 的曝品範圍變成 `productImg` 之後，
  就套用到既有的「沒填的代言人／簽名欄位，空間讓給商品圖」規則
  （跟 A 系列一致）。有填就正常顯示，維持預設值＝視同沒填。
- **A-2-1、A-3-1、B-3-4 的圖片欄位改名**，舊的工單暫存檔如果指名這些欄位，
  圖會跑到別格，重新拖一次即可。

---

## 8.7 維修頁改用「設計稿文字」

維修頁原本是用「一二三四五…」排到字數上限，看得出長度、但看不出這一格要放什麼。
現在多了一個文字模式 **「設計稿文字」（已設為預設）**，直接顯示 PS 設計稿上寫的字：

- 「品名一排最多8字」「促標最多7字內」「小字最多可9個字內」「逛逛去」「項目4字」…
- 一眼看得出這一欄是什麼、限幾個字，而且跟設計稿參考圖可以直接對照

做法：`tools/apply_css_design_text.py` 把 CSS 每個文字圖層的原文寫進 block.json 的
`designText`（**250 個欄位全部有了**）。這是額外欄位，跟 `default` 分開放，
所以匯入工單頁「沒填欄位時顯示什麼」的行為完全沒變。

配對用的是跟座標校正同一套幾何比對；圓標字那種垂直置中、走另一條計算路徑的，
再用「欄位名 ↔ 圖層名 + 由右到左順序」補上（`FIELD_TO_LAYER`）。

原本的三個模式都還在：

| 模式 | 用途 |
|---|---|
| 設計稿文字（預設） | 看設計稿原本的示意字，跟參考圖對照 |
| 字數上限 | 一二三四…排到上限，用來數有沒有被裁掉 |
| 版位預設 | 匯入工單沒填欄位時會看到的那個 |
| 留空 | 只看框 |

> 副區版位沒有對應的 CSS，所以沒有 `designText`，選「設計稿文字」時會自動退回
> 「字數上限」那套，不會變成空白。
